import { Outlet } from "react-router-dom";
const ListedBooks = () => {
    return (
        <div>
                
            <Outlet></Outlet>
            
        </div>
    );
};

export default ListedBooks;
